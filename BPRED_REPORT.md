# Предсказатель переходов SCR1: ранний BTB с уводом выборки — отчёт

Отчёт по доработке предсказателя переходов ядра Syntacore SCR1 (RV32IMC, конфигурация
`CFG=MAX`, плата Nexys A7-100T / xc7a100t, CPU_CLK 30 МГц). Работа велась от коммита
`4f8c1b5` (базовые фазы D0+D1 — динамический BHT) и завершается коммитом `e35b720`
на ветке `branch-predictor`.

Итоговый результат (полностью верифицирован в симуляции, CRC/PASS корректны, синтез
проходит по таймингу, прошито на плату):

| воркоад | такты, сток No-BPU/q2 | такты, итог | выигрыш |
|---|---|---|---|
| CoreMark  | 622 115 | 502 692 | **−19.2%** |
| Dhrystone | 307 074 | 262 066 | **−14.7%** |
| Qsort     | 534 493 | 456 358 | **−14.6%** |

---

## 1. Обзор архитектуры: конвейер и две точки предсказания

SCR1 (`CFG=MAX`) — трёхстадийный конвейер с регистрами между стадиями:

```
        ┌──────────────── IFU (выборка) ────────────────┐   ┌ IDU ┐   ┌──── EXU ────┐
 память → imem_addr_ff → [очередь 4 слова] → выход очереди → декод → рег → исполнение
   ▲          │                                   │                            │
   │      ① BTB (fetch)                    ② BTFN+BHT+RAS (head)         ③ резолв + обучение
   └──────────────────── редиректы (steer / late / EXU-flush) ──────────────────┘
```

Предсказание делается в **двух точках**:

- **① На выборке (рано):** ранний **BTB** по адресу выборки `imem_addr_ff` уводит поток
  выборки на цель ещё до того, как переход дойдёт до декода — убирает пузырь взятого
  перехода.
- **② На выходе очереди (поздно):** статический **BTFN** + динамический **BHT**
  (направление) + **RAS** (цель возвратов) предсказывают инструкцию, стоящую на выходе
  очереди.

**EXU — арбитр корректности.** Он пересчитывает реальный исход; при ошибке любого
предсказания флашит конвейер и уводит на правильный PC. Поэтому неверное предсказание =
потеря тактов, но **никогда не баг**. Это ключевой инвариант безопасности всей схемы.

---

## 2. Структуры предсказателя

### ① BTB — Branch Target Buffer (`scr1_pipe_btb.sv`) — ранний кэш целей

| | |
|---|---|
| **Зачем** | Знать цель взятого перехода на этапе выборки, чтобы увести поток рано. |
| **Хранит** | 256 записей `{valid, tag[21:0], target[31:0], safe, is_cond, bht_index[9:0]}`. |
| **Индекс** | `pc[9:2]` — по слову (директ-мэппинг, тег даёт точное совпадение → нет ложных попаданий). |
| **Читается** | Каждый такт выборки по `{imem_addr_ff, 2'b00}`. |
| **Пишется** | Из EXU при ретайре взятого прямого перехода/джампа: `pc=pc_curr_ff`, `target=jb_new_pc`, `safe`, `is_cond`. JALR/возвраты — не сюда (RAS). |
| **Recovery** | Не нужен (нет спекулятивного состояния). |
| **На FPGA** | distributed RAM (LUTRAM) для tag/target; ~238 LUT / ~512 FF. |

Поля `safe`, `is_cond`, `bht_index` — расширения для корректного и точного увода (см. §3–4).

### ② BHT — Branch History Table (`scr1_pipe_bht.sv`) — динамическое направление

| | |
|---|---|
| **Зачем** | Предсказывать taken/not-taken по истории (точнее статического BTFN). |
| **Хранит** | 1024 записи `{valid, 2-битный насыщающий счётчик}`. MSB счётчика = направление. |
| **Индекс** | `ifu_head_pc[10:1]` — по **полуслову** (нужен `pc[1]`, т.к. RVC-переход может быть в любой половине). |
| **Читается** | Порт 1 — на выходе очереди по `ifu_head_pc`; **порт 2** — на выборке по индексу из BTB (для гейтинга увода, §4). |
| **Пишется** | Из EXU на каждом разрешённом условном переходе. Обучение за 1 шаг: первая запись сразу в «слабо-taken/not-taken». |
| **Recovery** | Не нужен (обновление только по разрешённому исходу). |
| **На FPGA** | регистры (packed-векторы), ~7.5k ячеек — самый крупный элемент. |

### ③ BTFN — статический предсказатель (`scr1_pipe_bpred.sv`)

Чисто комбинационный, состояния не хранит. Джампы → всегда taken; условные →
backward-taken/forward-not-taken (по знаку смещения), либо направление BHT, если та
обучена. Цель = `ifu_head_pc + imm`. Даёт также признак `bp_is_branch`. Нужен для
холодного старта, пока BHT не обучена.

### ④ RAS — Return Address Stack (`scr1_pipe_ras.sv`)

Стек глубины 4 из `{valid, ra}`. **Call** (jal/jalr на x1/x5) → push адреса возврата;
**return** (jr/jalr на x1/x5) → pop; вершина = предсказанная цель возврата. Спекулятивно
обновляется на выходе очереди, **флашится на мисспредикте EXU**. Предсказывает цель
непрямых возвратов, которые BTB не кэширует.

---

## 3. Ранний увод выборки (early-BTB fetch steer)

Ключевая идея: при попадании BTB на этапе выборки увести поток выборки на цель
**без флаша очереди** — текущее слово с переходом дочитывается, а следующая выборка идёт
на цель (`imem_addr_next = btb_target`). Так на горячих циклах цель уже в очереди к моменту,
когда переход выходит из очереди → пузырь убран.

**Плумбинг корректности:**
- **`ifu_head_pc`** — «теневой PC» инструкции на выходе очереди; инвариант
  `ifu_head_pc == pc_curr_ff` (проверяется SVA).
- **`q_steered`** — 1 бит на полуслово очереди: «это слово было уведено». Решение об уводе
  принимается на запросе к памяти, а помечать слово надо при приходе ответа (запрос/ответ
  расцеплены), поэтому бит едет через маленький **steer-FIFO** (push на handshake запроса,
  pop на приёме ответа).
- **На выходе очереди** развилка по `q_steered`:
  - уведённый переход + BHT-taken → **commit**: флаша нет, цель уже в очереди (выигрыш);
  - уведённый + BHT-not-taken → **misfetch**: флаш на fall-through (не мисспредикт);
  - не уведённый + taken → поздний редирект D1 (полный пузырь).

**RVC-безопасность (важно — книга RV32I этого не покрывает).** Увод корректен, только если
переход **заканчивается на границе слова** (иначе рядом в том же слове сидит fall-through,
который увод выполнил бы ошибочно). Поэтому уводятся только **safe**-переходы
(`safe = ~(pc[1] ^ rvc)` — RVI-aligned или RVC в старшей половине), бит `safe` хранится в
BTB. Небезопасные (RVC-low, RVI-straddling — ~65% взятых переходов на CoreMark) идут через
поздний редирект D1. Их полный охват требует предекода (отдельная, отложенная задача).

**Невыровненные цели.** Если цель невыровнена (`target[1]=1`), её младшую половину надо
пропустить: бит `target[1]` едет через steer-FIFO и на ответе branch-слова выставляет
`new_pc_unaligned` для слова цели. Это позволило уводить и на невыровненные цели без флаша.

**Никаких дропов полуслов.** В отличие от предыдущей (неудачной) попытки, схема **не
отбрасывает полуслова** — именно поэтому она CRC-корректна, тогда как drop-based подход молча
портил данные.

---

## 4. Fetch-side BHT-гейтинг (направление на выборке)

По модели книги `taken = BTB.valid & counter`, направление должно участвовать в раннем
редиректе. Изначально увод гейтился только по BTB-hit — и уведённый переход, который BHT
затем называла not-taken, стоил **misfetch-флаша** (пузырь, которого при гейтинге на выборке
не было бы). Замер misfetch: CoreMark 5256, Qsort 9654, Dhrystone 11692.

Реализовано: `steer = btb_hit & safe & (~is_cond | (bht_valid & bht_taken))` — джампы
уводятся всегда, условные — только при обученном taken-счётчике. Индекс BHT на выборке
недоступен (нужен `pc[1]`), поэтому индекс перехода и бит `is_cond` **хранятся в записи BTB**,
а BHT получила **второй комбинационный порт чтения** по этому индексу.

Эффект: **`steer_misfetch → 0` на всех воркоадах**; мисспредикты не изменились; instret тот же
(работа та же). Выигрыш (особенно на dhrystone, где misfetch был больше commit):

| воркоад | до гейтинга | после гейтинга |
|---|---|---|
| CoreMark  | −18.4% | **−19.2%** |
| Dhrystone | −7.9%  | **−14.7%** |
| Qsort     | −13.4% | **−14.6%** |

---

## 5. Глубина очереди выборки как конфиг-параметр

2-словная очередь IFU (зашитый в апстриме `localparam`) — доминирующее узкое место
CoreMark: она не успевает скрыть задержку выборки/дозаливку после редиректа, фронтенд
голодает (~12% тактов). Углубление до 4 слов практически обнуляет это голодание
(`bub_other` 72 238 → 92 на CoreMark).

Глубина выведена как честный параметр `SCR1_IFU_QUEUE_SIZE_WORD` в
`scr1_arch_description.svh` (=4), рядом с RAS/BHT/BTB; `SCR1_TXN_CNT_W` теперь производится
из неё (чтобы глубины ≥8 не подвешивали IFU). 4 слова — измеренная точка насыщения.

**Очередь и предсказатель комплементарны:** очередь чинит голодание (`bub_other`),
предсказатель — пузыри взятых переходов (`bub_bp`); эффекты складываются.

---

## 6. Результаты и атрибуция (симуляция, verilator, точные такты)

База — сток No-BPU/q2. Все прогоны корректны (CoreMark CRC `0xe714`/`0x1fd7`, qsort SORTED,
dhrystone PASS; 60/60 в полном прогоне).

| | CoreMark | Dhrystone | Qsort |
|---|---|---|---|
| **итого (очередь-4 + предсказатель)** | **−19.2%** | **−14.7%** | **−14.6%** |
| очередь 2→4 сама | −11.8% | −3.4% | −5.8% |
| предсказатель-только (q2) | −6.2% | −4.3% | −8.1% |
| предсказатель при q4 (vs No-BPU/q4) | −8.4% | −11.6% | −9.4% |

**Каждый воркоад упирается в своё, и один из двух рычагов его закрывает:**
- CoreMark — **frontend-bound** → доминирует очередь;
- Qsort — **branch-bound** (MPKI ~49) → доминирует предсказатель, и предсказатель-только уже
  берёт ≥8%;
- Dhrystone — **back-end-bound** → оба рычага помогают скромнее (наш самый слабый кейс).

Метрика — реальные такты (счётчики `BP_PROFILE` в тестбенче, под `SCR1_BP_PROFILE`; тики
`rdtime` не используются — они тактируются от 1 МГц RTC, отдельно от ядра).

---

## 7. Синтез (post-synthesis, Vivado 2019.1, xc7a100t, CPU_CLK 30 МГц)

- **Тайминг MET**: WNS +1.317 нс (задаётся DDR2 MIG, не ядром).
- Цепочка увода (BTB→BHT→imem_addr_next) села на путь выборки, но с запасом **+6.129 нс**
  (без BHT-гейтинга было +6.684; гейтинг стоил ~0.55 нс).
- Площадь относительно стока: BTB +238 LUT/+512 FF; очередь-4 +84 LUT/+83 FF; BHT-гейтинг
  +966 LUT/+265 FF (в основном второй порт чтения BHT). BTB инферится как distributed RAM.
- **Размена тактов на Fmax нет.** Конвейеризация чтения на 30 МГц не требуется.

*(Оговорка: post-synthesis — оценка без трассировки; точное board-число требует
place & route.)*

---

## 8. Проверка на плате

Дизайн имплементирован, битстрим собран, бутлоадер SCBL впрыснут в boot-SRAM
(`mem_update.tcl` → `nexys4ddr_scr1_new.bit`), плата запрограммирована по JTAG
(FPGA сконфигурировалась, DONE=HIGH). ILA из сборки убрана (её пробы ссылались на
изменённые сигналы; см. §10). Проверка — по UART (`/dev/ttyUSB1`, 115200 8N1) через меню
бутлоадера + заливка теста в TCM по XMODEM.

---

## 9. Честные ограничения

- **Предсказатель-только ≥8% — зависит от воркоада:** достигается на ветвистом qsort
  (−8.1% при очереди-2), но CoreMark предсказателем-только упирается в ~6.2% — там держит
  2-словная очередь (параметр фронтенда/ядра).
- **RVC-стена:** ~65% взятых переходов не заканчиваются на границе слова (7% RVC-low +
  58% RVI-unaligned) и не уводятся ранним BTB без **предекода** входящего слова — это
  осознанно отложенный крупный редизайн.
- Числа синтеза — post-synth; board-замер dhrystone (−9% на −O2, −4.9% на −O3 vs сток)
  ниже симуляционного из-за иных флагов компиляции и метода замера — см. §6, метрика тактов.

---

## 10. Карта изменений (файлы)

Новые: `src/core/pipeline/scr1_pipe_btb.sv`, `sim/tests/benchmarks/qsort/`.
Правки: `scr1_pipe_ifu.sv` (увод, steer-FIFO, q_steered, гейтинг, очередь-параметр),
`scr1_pipe_bht.sv` (2-й порт), `scr1_pipe_exu.sv` (каналы обучения BTB),
`scr1_pipe_top.sv` (провода), `scr1_arch_description.svh` (define `SCR1_BP_BTB`,
`SCR1_IFU_QUEUE_SIZE_WORD`), `src/tb/scr1_top_tb_ahb.sv` (профилировщик `SCR1_BP_PROFILE`).

Коммиты на `branch-predictor` (от `4f8c1b5`):
`fc52169` ранний BTB (Level B) + профилировщик · `c14e0cd` невыровненные цели ·
`1706c0c` глубина очереди как параметр (=4) · `9cb4fab` бенчмарк qsort ·
`e35b720` fetch-side BHT-гейтинг.
